# AGENTS.md — AI 代理最小作业卡

你面对的是一个「只追加」的视频提示词库。唯一数据源：`视频提示词库-多模型.html`（`script#data` 条目数组 + `script#meta` 规则与指纹）。完整手册见 `README.md`（唯一手册；坑与防错清单见 README §8）。

## 不可破的约束

1. 只追加，不删改老条目；不改文件名，不 bump 版本。
2. `original` 逐字不可变（SHA-256 指纹校验）。
3. 一切写入只能通过 `python 追加提示词.py "视频提示词库-多模型.html" 新条目.json`；禁止手改 data/meta 块。
4. 仓库 `main` = 唯一真源，git 历史即备份；改前拉最新，改后立刻推回并回读校验。
5. 扩展区（通用区/教程区）：通用素材（运镜词典、镜头参数、构图模板等）与教程（完整工作流案例）由你转换后用 `扩展区工具.py` 写入 general/tutorials 块（init 首次/append 追加/verify 校验）；用户同样只给原始素材（md/网页/截图随意），归类与结构化全由你完成。原第 5 条顺延为第 6 条。
6. 源提示词只允许机器搬运（整文件复制/直链下载），严禁人肉转录；大批次走 `.url` 直链 + SHA-256（README §6）。
7. 严禁把 token / 密钥写入任何提交文件。

## 标准作业

1. 把拿到的提示词整理成 9 字段 JSON 数组：`model`（系列+空格+版本）/ `cat` / `sub` / `name`（中文）/ `desc`（中文）/ `tags`（数组 3–6 个）/ `lang` / `original`（逐字保留）/ `src`；外文条目另加 `zh` 中文译文。
2. 分类优先复用 README §4 的现有 cat/sub；新建子类用「X·Y」式。
3. 能跑 Python：拉最新库 → 追加 → `python 库维护工具.py verify` 全绿 → 推送 main（提交信息 `追加 N 条：<简述>`）→ 回读远端再 verify。
4. 用户只需说模型名（如 Seedance 2.0 / 2.5 / H3 / 可灵 O3 / 可灵 3.0）+ 给原始提示词（md / txt / 截图 / 粘贴文本 / 链接，随意）；解析、分类、中文名/简介/标签/译文、查重全部由你完成，生成 9 字段 JSON（§3）——用户不手填任何字段、不参与分类。JSON 就绪后：能跑 Python 走 `追加提示词.py`；跑不了就把 JSON 提交到 `待入库/`（小批次单文件即可，≥50 条再分片），大文件改两行 `.url` 文件（第 1 行直链、第 2 行 SHA-256）。CI 自动入库并同步 README 统计（`同步README统计.py`），结果看 Actions 绿勾与 `运行记录/最近入库.log`。
6. 校验红灯 = 零写入：修 JSON 重来；已推坏就 `git revert`，不要 force push。

## 网页布局铁律（改 HTML 前必读，用户定稿审美）

- **高信息密度**：一排多列网格、同类卡片高度完全统一（固定 height + flex 分区 + 卡内滚动）。
- 教程步骤卡**分两档自动渲染**（JS 判定 `!s.prompt || (s.role||'').length<110`）：
  - **flat 矮横条**（无提示词或解释<110字）：通栏、图左文右横排、~150px、长文内滚——省页面空间；
  - **400px 三列大卡**（解释≥110字）：thead 固定、tbody 限高 150px 内滚、提示词 pane flex:1 占满剩余空间。
- **卡内绝不允许大片留白**：正文区与提示词区「谁有内容谁占位」，短内容的卡由提示词区自动撑满（用户 2026-08-24 批判过正文区拉伸导致的空白）。
- 用户反感：满宽单列、高度参差、折叠交互、大图（仓库体积敏感，教程图必须 160px WebP 级）。
- **任何布局改动必须同步**：README 更新日志 + 本文件本节，否则后续会话不知道规则会改回去。

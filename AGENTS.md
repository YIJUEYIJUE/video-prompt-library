# AGENTS.md — AI 代理最小作业卡

你面对的是一个「只追加」的视频提示词库。唯一数据源：`视频提示词库-多模型.html`（`script#data` 条目数组 + `script#meta` 规则与指纹）。完整手册见 `README.md`（唯一手册；坑与防错清单见 README §8）。

## 不可破的约束

1. 只追加，不删改老条目；不改文件名，不 bump 版本。（勘误类修改须在 meta.勘误登记 留痕：日期+条目+类型+详情；删除重复条目属"语义去重"类勘误）
2. `original` 逐字不可变（SHA-256 指纹校验）。OCR 明显错字的合理修正必须在 commit message 与变更日志里列明原文→修正。
3. 一切写入只能通过 `python 追加提示词.py "视频提示词库-多模型.html" 新条目.json`；禁止手改 data/meta 块。
4. 仓库 `main` = 唯一真源，git 历史即备份；改前拉最新，改后立刻推回并回读校验。
5. 扩展区（通用区/教程区）：通用素材（运镜词典、镜头参数、构图模板等）与教程（完整工作流案例）由你转换后用 `扩展区工具.py` 写入 general/tutorials 块（init 首次/append 追加/verify 校验）；用户同样只给原始素材（md/网页/截图随意），归类与结构化全由你完成。**general/tutorials 批次禁止投给 追加提示词.py（会因缺 model 字段静默失败）——CI 已按 batch 是否含 model 字段自动分流，本机同样遵守。**
6. 源提示词只允许机器搬运（整文件复制/直链下载），严禁人肉转录；大批次走 `.url` 直链 + SHA-256（README §6）。
7. 严禁把 token / 密钥写入任何提交文件。
8. verify 是 fail-closed：meta 块不可解析或指纹登记缺失时必须红灯退出，不许静默跳过校验（2026-08-26 对抗式审查修复）。

## 标准作业

1. 把拿到的提示词整理成 9 字段 JSON 数组：`model`（系列+空格+版本）/ `cat` / `sub` / `name`（中文）/ `desc`（中文）/ `tags`（数组 3–6 个）/ `lang` / `original`（逐字保留）/ `src`。**译文规则（用户 2026-08-24 定）：英文源提示词必须补 `zh` 中文译文；中文源不补**（`lang:'zh'`、无 zh，网页单页签"原文 中文"）。
2. 分类优先复用 README §4 的现有 cat/sub；新建子类用「X·Y」式。
3. 能跑 Python：拉最新库 → 追加 → `python 库维护工具.py verify` 全绿 → 推送 main（提交信息 `追加 N 条：<简述>`）→ 回读远端再 verify。
4. 用户只需说模型名（如 Seedance 2.0 / 2.5 / H3 / 可灵 O3 / 可灵 3.0）+ 给原始提示词（md / txt / 截图 / 粘贴文本 / 链接，随意）；解析、分类、中文名/简介/标签/译文、查重全部由你完成，生成 9 字段 JSON（§3）——用户不手填任何字段、不参与分类。JSON 就绪后：能跑 Python 走 `追加提示词.py`；跑不了就把 JSON 提交到 `待入库/`（小批次单文件即可，≥50 条再分片），大文件改两行 `.url` 文件（第 1 行直链、第 2 行 SHA-256）。CI 自动入库并同步 README 统计（`同步README统计.py`），结果看 Actions 绿勾与 `运行记录/最近入库.log`。
5. 校验红灯 = 零写入：修 JSON 重来；已推坏就 `git revert`，不要 force push。

## 教程入库铁律（2026-08-24 Santiago 教训，用户明令记住）

- **一次只入库一篇教程**：翻译→组装→校验→推送→用户验收，全流程闭环后才开下一篇。单篇上限约 70 翻译条目/25K 正文/20 提示词
- **入库前全字段终扫**（非抽查）：标题无残片且<60字、译文无「暂无/编号残留」且≥原文12%、正文无中间产物
- **源提示词逐字保留是红线**：original/prompt 字段不许被翻译覆盖、截断、污染
- 超长提示词（>7K字符）必须按镜头分段翻译后拼接，拼接后核对分块数与原文一致
- 手动补翻先预览落点字段再写库（第15步标题混入翻译字典就是反例）
- 详细管线坑清单见 skill: video-prompt-library-workflow「教程入库铁律」

## 网页布局铁律（改 HTML 前必读，用户定稿审美）

- **高信息密度**：一排多列网格、同类卡片高度完全统一（固定 height + flex 分区 + 卡内滚动）。
- 教程步骤卡**分两档自动渲染**（JS 判定 `!s.prompt || (s.role||'').length<110`）：
  - **flat 矮横条**（无提示词或解释<110字）：通栏、图左文右横排、~150px、长文内滚——省页面空间；
  - **400px 三列大卡**（解释≥110字）：thead 固定、tbody 限高 150px 内滚、提示词 pane flex:1 占满剩余空间。
- **卡内绝不允许大片留白**：正文区与提示词区「谁有内容谁占位」，短内容的卡由提示词区自动撑满（用户 2026-08-24 批判过正文区拉伸导致的空白）。
- **图片档位两档（用户 2026-08-24 定）**：默认最小档 = 长边 160px WebP（教程区等）；用户明确说"压到正常"的批次用正常档 = 长边 256px WebP q75（通用区器材图即此档，56 张仅 312KB）。
- 通用区支持 `img` 字段（dataURL WebP）：卡片右浮 132×138 cover 裁切，`.gbody` BFC 包裹绕排；`lang:'zh'` 条目渲染单页签。
- **卡片同行等高：网格 align-items:stretch**（用户 2026-08-24 定："同一列高度跟最高的统一"）。行高由该行最高卡决定（竖图卡），同行矮卡被拉伸、内部 gbody flex 弹性填充；行与行之间高度可不同。**图片必须完整显示**（width:150px + height:auto，禁止 object-fit:cover 裁切——用户明确"不能看到全部就不行"）。设备名称必须中文大字+英文小字副行（34 条已补译：蔡司/阿莱/库克/潘纳维申/八羽怪/RED 迅猛龙等）。#grid（主库）与 #ggrid（通用区）都是 stretch。
- **分区结构（2026-08-24 定稿）**：顶部 tab = 提示词库 / 通用区 / 教程区（三区）。通用区内部两级分类栏：第一行词典行（全部词典 / 运镜词典 / 器材词典，像主库 Seedance/海螺模型按钮），第二行分组行（选中词典后显示其 sub 分组）。`cat:'器材词典'` 条目归器材词典词典行下，默认全部混合、可一键切换——用户要求"在通用区的分类里区分开，不混着看"。
- 用户反感：满宽单列、高度参差、折叠交互、大图。
- **任何布局改动必须同步**：README 更新日志 + 本文件本节，否则后续会话不知道规则会改回去。
- **视觉微调记录（2026-08-28）**：分类按钮 `.cbtn.on` 激活态 = 实底白字，底色用专用变量 `--acc-solid`（浅 `#3E86D6` / 深 `#5E9FE8`——用户嫌 `--acc` 深蓝太重，专门调浅一档，全局 `--acc` 不动）；提示词页签 `.pt` 字号 11.5px。改这两处前先想清楚是否真要回退——这是用户"先调整看看"后保留的定稿。

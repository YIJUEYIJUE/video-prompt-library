# 待入库（收件箱）

把符合仓库 README §3 格式的 `*.json`（9 必填字段，数组可多条；外文条目必带 `zh` 译文）提交到本目录，GitHub Actions 会自动校验并追加进 `视频提示词库-多模型.html`，成功后把 JSON 移入 `已入库/`。

- Actions 绿勾 = 已入库；红叉 = 一条都没写入，看日志修改后重新提交。
- 文件名建议：`来源_日期.json`，如 `用户直供_2026-08-22.json`。
- 校验不过的常见原因见 README §7。
